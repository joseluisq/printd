sudo: false

language: node_js

node_js:
    - "12"

env:
    - NODE_ENV=development

git:
    depth: 1

cache:
    yarn: true
    directories:
        - node_modules

notifications:
    email: false

services:
    - xvfb

addons:
    chrome: stable

before_script:
    - export DISPLAY=:99.0

script:
    - yarn test:bundle
